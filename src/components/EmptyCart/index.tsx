import { FC } from "react";
import { Text } from "@vkontakte/vkui";

const EmptyCart: FC = () => {
  return <Text style={{ textAlign: "center" }}>The cart is Emply =(</Text>;
};
export default EmptyCart;
